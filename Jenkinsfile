pipeline {
agent any
       stages{
           stage('Pipeline Bookshelf') {
               steps{
                   echo " Inicio Pipeline mvn clean package"
               }
           }
       }
}